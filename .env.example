# ============================================
# LazyDevs App - Environment Variables
# ============================================
# Copy this file to .env.local and fill in your actual values
# Never commit .env.local to version control!

# ============================================
# App Configuration
# ============================================
# Base URL of your application
# Development: http://localhost:3000
# Production: https://your-domain.com
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ============================================
# Firebase Configuration
# ============================================
# Get these from: Firebase Console > Project Settings > General
# Create a new web app if you haven't already
NEXT_PUBLIC_FIREBASE_API_KEY=AIzaSyXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=123456789012
NEXT_PUBLIC_FIREBASE_APP_ID=1:123456789012:web:abcdef123456

# Firebase Admin SDK (for server-side operations)
# Get this from: Firebase Console > Project Settings > Service Accounts > Generate new private key
# IMPORTANT: Keep this secret! Never expose in client-side code
FIREBASE_ADMIN_PROJECT_ID=your-project-id
FIREBASE_ADMIN_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project-id.iam.gserviceaccount.com
FIREBASE_ADMIN_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYourPrivateKeyHere\n-----END PRIVATE KEY-----\n"

# ============================================
# Token Encryption
# ============================================
# Used to encrypt OAuth tokens before storing in database
# CRITICAL: Generate a strong random key for production!
# You can generate one with: openssl rand -base64 32
# Must be at least 32 characters
NEXT_PUBLIC_ENCRYPTION_KEY=your-secure-encryption-key-change-in-production-min-32-chars

# ============================================
# Jira OAuth 2.0
# ============================================
# Setup instructions: https://developer.atlassian.com/cloud/jira/platform/oauth-2-3lo-apps/
# 1. Go to https://developer.atlassian.com/console/myapps/
# 2. Create a new app > OAuth 2.0 integration
# 3. Add scopes: read:jira-work, write:jira-work, offline_access
# 4. Add callback URL: http://localhost:3000/api/integrations/jira/callback
JIRA_CLIENT_ID=your-jira-client-id
JIRA_CLIENT_SECRET=your-jira-client-secret
JIRA_REDIRECT_URI=http://localhost:3000/api/integrations/jira/callback

# ============================================
# Slack OAuth 2.0
# ============================================
# Setup instructions: https://api.slack.com/authentication/oauth-v2
# 1. Go to https://api.slack.com/apps
# 2. Create New App > From scratch
# 3. Add Bot Token Scopes: chat:write, channels:read, channels:join, files:write
# 4. Add User Token Scopes: chat:write
# 5. Add Redirect URL: http://localhost:3000/api/integrations/slack/callback
SLACK_CLIENT_ID=your-slack-client-id
SLACK_CLIENT_SECRET=your-slack-client-secret
SLACK_REDIRECT_URI=http://localhost:3000/api/integrations/slack/callback

# ============================================
# GitHub OAuth
# ============================================
# Setup instructions: https://docs.github.com/en/apps/oauth-apps/building-oauth-apps
# 1. Go to https://github.com/settings/developers
# 2. New OAuth App
# 3. Authorization callback URL: http://localhost:3000/api/integrations/github/callback
# 4. Scopes will be requested: repo, write:org, read:user
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret
GITHUB_REDIRECT_URI=http://localhost:3000/api/integrations/github/callback

# ============================================
# Notion OAuth
# ============================================
# Setup instructions: https://developers.notion.com/docs/authorization
# 1. Go to https://www.notion.so/my-integrations
# 2. Create new integration > Public integration
# 3. Add Redirect URI: http://localhost:3000/api/integrations/notion/callback
# 4. Set capabilities as needed (read content, update content, insert content)
NOTION_CLIENT_ID=your-notion-client-id
NOTION_CLIENT_SECRET=your-notion-client-secret
NOTION_REDIRECT_URI=http://localhost:3000/api/integrations/notion/callback

# ============================================
# ClickUp OAuth
# ============================================
# Setup instructions: https://clickup.com/api/developer-portal/authentication/
# 1. Go to https://app.clickup.com/settings/apps
# 2. Create new app
# 3. Add Redirect URL: http://localhost:3000/api/integrations/clickup/callback
CLICKUP_CLIENT_ID=your-clickup-client-id
CLICKUP_CLIENT_SECRET=your-clickup-client-secret
CLICKUP_REDIRECT_URI=http://localhost:3000/api/integrations/clickup/callback

# ============================================
# Voice Processing API (Optional)
# ============================================
# If you're using an external voice processing service
# VOICE_API_URL=https://your-voice-api.com
# VOICE_API_KEY=your-voice-api-key

# ============================================
# Production Notes
# ============================================
# When deploying to production:
# 1. Update NEXT_PUBLIC_APP_URL to your production domain
# 2. Generate a new, strong NEXT_PUBLIC_ENCRYPTION_KEY
# 3. Update all OAuth redirect URIs to production URLs
# 4. Reconfigure OAuth apps with production redirect URLs
# 5. Never commit .env.local or .env.production to git
# 6. Use your hosting provider's environment variable system
# 7. Enable 2FA on all third-party service accounts
# 8. Rotate credentials regularly
# 9. Monitor OAuth token usage and access logs
# 10. Set up proper CORS and CSP headers
